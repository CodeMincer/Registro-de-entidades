<form method="post" action="{{ path('app_flores_delete', {'id': flore.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ flore.id) }}">
    <button class="btn">Delete</button>
</form>
