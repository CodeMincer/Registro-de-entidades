<form method="post" action="{{ path('app_flores_delete', {'id': flore.id}) }}" onsubmit="return confirm('¿Eliminar item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ flore.id) }}">
    <button class="btn btn-danger">Eliminar</button>
</form>
