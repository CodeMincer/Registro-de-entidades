{% extends 'base.html.twig' %}

{% block title %}Editar compra{% endblock %}

{% block body %}
    <h1>Editar compra</h1>

    {{ include('compras/_form.html.twig', {'button_label': 'Actualizar'}) }}

<div class="d-flex">
    {{ include('compras/_delete_form.html.twig') }}
    <a href="{{ path('app_compras_index') }}" class="btn btn-secondary">Regresar</a>
</div>
{% endblock %}
