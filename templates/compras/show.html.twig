{% extends 'base.html.twig' %}

{% block title %}Compras{% endblock %}

{% block body %}
    <h1>Compras</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ compra.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_compras_index') }}">back to list</a>

    <a href="{{ path('app_compras_edit', {'id': compra.id}) }}">edit</a>

    {{ include('compras/_delete_form.html.twig') }}
{% endblock %}
