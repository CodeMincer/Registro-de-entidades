{% extends 'base.html.twig' %}

{% block title %}Compra{% endblock %}

{% block body %}
    <h1>Datos de la compra</h1>

    <table class="table table-dark table-striped">
        <thead>
            <tr>
                <th>Id</th>
                <td>{{ compra.id }}</td>
            </tr>
        </thead>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ compra.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_compras_edit', {'id': compra.id}) }}" class="btn btn-primary">Editar</a>

    <div class="d-flex">
        {{ include('compras/_delete_form.html.twig') }}
        <a href="{{ path('app_compras_index') }}" class="btn btn-secondary">Regresar</a>
        <style>
            .d-flex{
                margin-top: 10px;
            }
        </style>
    </div>

{% endblock %}
