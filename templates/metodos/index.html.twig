{% extends 'base.html.twig' %}

{% block title %}Metodos index{% endblock %}

{% block body %}
    <h1>Metodos index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Tipo</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for metodo in metodos %}
            <tr>
                <td>{{ metodo.id }}</td>
                <td>{{ metodo.tipo }}</td>
                <td>
                    <a href="{{ path('app_metodos_show', {'id': metodo.id}) }}">show</a>
                    <a href="{{ path('app_metodos_edit', {'id': metodo.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_metodos_new') }}">Create new</a>
{% endblock %}
