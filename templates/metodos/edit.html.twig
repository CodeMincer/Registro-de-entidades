{% extends 'base.html.twig' %}

{% block title %}Editar Métodos{% endblock %}

{% block body %}
    <h1>Editar método</h1>

    {{ include('metodos/_form.html.twig', {'button_label': 'Actualizar'}) }}

    <div class="d-flex">
        {{ include('metodos/_delete_form.html.twig') }}
        <a href="{{ path('app_metodos_index') }}" class="btn btn-secondary">Regresar</a>
    </div> 
{% endblock %}
