<form method="post" action="{{ path('app_metodos_delete', {'id': metodo.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ metodo.id) }}">
    <button class="btn">Delete</button>
</form>
