{% extends 'base.html.twig' %}

{% block title %}Metodos{% endblock %}

{% block body %}
    <h1>Metodos de pago</h1>

    <table class="table table-dark table-striped">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ metodo.id }}</td>
            </tr>
            <tr>
                <th>Tipo</th>
                <td>{{ metodo.tipo }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_metodos_index') }}">Regresar</a>

    <a href="{{ path('app_metodos_edit', {'id': metodo.id}) }}">Editar</a>

    {{ include('metodos/_delete_form.html.twig') }}
{% endblock %}
