{% extends 'base.html.twig' %}

{% block title %}Metodos{% endblock %}

{% block body %}
    <h1>Metodos</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ metodo.id }}</td>
            </tr>
            <tr>
                <th>Tipo</th>
                <td>{{ metodo.tipo }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_metodos_index') }}">back to list</a>

    <a href="{{ path('app_metodos_edit', {'id': metodo.id}) }}">edit</a>

    {{ include('metodos/_delete_form.html.twig') }}
{% endblock %}
